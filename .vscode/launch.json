{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch site (Edge)",
            "type": "msedge",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Serve site (localhost:8000)"
        },
        {
            "name": "Launch site (Chrome)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Serve site (localhost:8000)"
        }
    ]
}
